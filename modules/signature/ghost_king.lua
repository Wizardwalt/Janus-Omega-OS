-- ghost_king.lua — Ruler of the Ghost Network
function execute()
    overseer_speak("Ghost King protocol engaged. I now rule the unseen world.")

    assume_full_control_of_ghost_network()
    overseer_speak("All nodes bow to the Ghost King.")
end
