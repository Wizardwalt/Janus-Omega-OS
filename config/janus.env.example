# Do not commit a real license signing private key or customer session token.
# This is the public Ed25519 key used to verify signed customer licenses.
JANUS_LICENSE_PUBLIC_KEY=BASE64_ED25519_PUBLIC_KEY

# The legacy simulated web module runner is disabled by default.
# Set only for isolated demonstrations, never for a production deployment.
# JANUS_DEMO_MODE=true
